---
description: Create a new Claude Code hook with guided setup — select event, matcher, behavior, generate script, and register in hooks.json.
argument-hint: <hook-name> [--event <event>] [--matcher <tool>]
---

# Create Hook

Invoke the create-hook skill to create a new hook.

## Process

Load and follow the skill:

```
lore/skills/create-hook/SKILL.md
```
