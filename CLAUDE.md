# Lore development

Read [AGENTS.md](AGENTS.md) for the provider-neutral repository guide and verification commands. The source is `lore/`; generated packages and installed copies are not the canonical codebase.

Claude Code is one runtime adapter. Use its available tools without assuming every other host exposes the same APIs, model names, hook events, or configuration paths. Preserve user-owned local changes and prior authorizations.
